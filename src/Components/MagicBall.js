import React from 'react';

function MagicBall() {
  const positiveResponse = ['Yes', 'Looks Likely', 'Absolutely', 'Very Likely'];
  const negativeResponse = ['No', "Don't Count On It", 'No Way', 'Not Likely'];
  const neutralResponse = ['Maybe', 'Outlook Unknown', 'Ask Again Later'];
  return <div></div>;
}

export default MagicBall;
